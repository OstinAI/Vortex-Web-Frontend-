steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/vortex-frontend', '.']
images:
  - 'gcr.io/$PROJECT_ID/vortex-frontend'
options:
  logging: CLOUD_LOGGING_ONLY
